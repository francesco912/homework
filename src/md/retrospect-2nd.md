# 두 번째 회고

이번 주에는 HTML과 CSS로 아바타(Avatar) 레이아웃을 구현했다.  
`<img>` 태그를 사용해 콘텐츠 이미지를 삽입하고, `loading="lazy"` 속성으로 이미지 성능 최적화를 적용했다.  
`float`과 `position:absolute`를 이용해 아바타를 배치하고, 상태 점을 겹쳐 온라인/오프라인을 표현했다.

CSS 배치가 아직 익숙하지 않았지만, 직접 구현하며 레이아웃 구조를 이해할 수 있었다.
